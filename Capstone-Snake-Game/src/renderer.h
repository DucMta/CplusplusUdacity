#ifndef RENDERER_H
#define RENDERER_H

#include <vector>
#include "SDL.h"
#include "snake.h"

class Renderer {
 public:
  Renderer(const std::size_t screen_width, const std::size_t screen_height,
           const std::size_t grid_width, const std::size_t grid_height);
  ~Renderer();

  //DucLH12
  void Render(Snake const snake, Snake const snake2, std::vector<SDL_Point> const &food, std::vector<SDL_Point> &obstacle);//snake2/food_v2
  //DucLH12
  void UpdateWindowTitle(int score, int fps, int scoreLastTime);

 private:
  SDL_Window *sdl_window;
  SDL_Renderer *sdl_renderer;

  const std::size_t screen_width;
  const std::size_t screen_height;
  const std::size_t grid_width;
  const std::size_t grid_height;
};

#endif